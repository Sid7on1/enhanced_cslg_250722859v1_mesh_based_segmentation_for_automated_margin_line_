# requirements.txt

# Python dependencies
python_version = "3.9"

# Computer Vision dependencies
torch = "1.12.1"
numpy = "1.22.3"
pandas = "1.4.2"
scipy = "1.7.3"
scikit-image = "0.19.2"
opencv-python = "4.5.5.64"
matplotlib = "3.5.1"
seaborn = "0.11.2"
plotly = "5.7.0"

# Mesh-based segmentation dependencies
meshlab = "2022.12"
trimesh = "3.10.5"
scikit-geometry = "0.113.1"

# Constrained and Splprep dependencies
scipy-splines = "0.3.0"
scipy-integrate = "1.7.3"

# Predicted and Other dependencies
scikit-learn = "1.0.2"
scipy-stats = "1.7.3"

# Semantic and Line dependencies
scipy-spatial = "1.7.3"
scipy-ndimage = "1.7.3"

# Edge dependencies
scipy-signal = "1.7.3"

# Logging and configuration dependencies
loguru = "0.7.0"
configparser = "5.2.0"

# Testing and integration dependencies
pytest = "6.2.5"
pytest-cov = "2.12.1"
pytest-mock = "3.6.1"
requests = "2.28.1"

# Performance and optimization dependencies
numba = "0.56.4"
cython = "0.29.32"

# Type hints and documentation dependencies
mypy = "0.931"
sphinx = "4.2.0"
sphinx-autodoc-typehints = "1.12.0"

# Thread safety and concurrent access dependencies
threading = "3.9.7"
concurrent.futures = "3.9.7"

# Data persistence and storage dependencies
pandas-datareader = "0.10.0"
pandas-gbq = "0.18.0"

# Event handling and state management dependencies
eventlet = "0.33.0"
greenlet = "0.4.17"

# Resource cleanup and management dependencies
contextlib2 = "0.7.4"
tempfile = "3.9.7"

# Constants and configuration dependencies
pyyaml = "6.0"
toml = "0.10.2"

# Other dependencies
wheel = "0.37.1"
setuptools = "58.1.0"
pip = "22.2.2"